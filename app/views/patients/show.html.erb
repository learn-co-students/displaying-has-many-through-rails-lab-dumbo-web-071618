<h1><%= @patient.name %> </h1>
<h3>Appointments:</h3>
<% @patient.appointments.each do |appt| %>
  <li><%= link_to appt.appointment_datetime.strftime("%B %e, %Y at %k:%M"),appointment_path(appt) %> with <%= link_to appt.doctor.name, doctor_path(appt.doctor) %></li>
<% end %>
