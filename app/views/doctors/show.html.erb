<h1>Doctor: <%= @doctor.name %></h1>
<h3>Appointments</h3>

<% @doctor.appointments.each do |e| %>
  <li><%= e.readable_datetime %> <%= link_to e.patients_name, patient_path(e.patient) %></li>
  <% end %>