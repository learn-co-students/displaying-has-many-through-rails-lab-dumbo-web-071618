<h1><%= @doctor.name %></h1>
<h3>Appointments: </h3>
<% @doctor.appointments.each do |appt| %>
  <li><%= appt.appointment_datetime.strftime("%B %e, %Y at %k:%M") %> with <%= link_to appt.patient.name,patient_path(appt.patient) %> in <%= @doctor.department %></li>
<% end %>
